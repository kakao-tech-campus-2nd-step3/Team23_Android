<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/expenseChipGroup_chipGroup"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:checkableBehavior="single"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.chip.Chip
            android:id="@+id/allExpense_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="@string/expenseList_allExpense"
            app:checkedIconVisible="false"
            app:chipBackgroundColor="@drawable/expense_list_chip_color" />

        <com.google.android.material.chip.Chip
            android:id="@+id/unconfirmedExpense_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="false"
            android:text="@string/expenseList_unconfirmedExpense"
            app:checkedIconVisible="false"
            app:chipBackgroundColor="@drawable/expense_list_chip_color" />

        <com.google.android.material.chip.Chip
            android:id="@+id/confirmedExpense_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="false"
            android:text="@string/expenseList_confirmedExpense"
            app:checkedIconVisible="false"
            app:chipBackgroundColor="@drawable/expense_list_chip_color" />

    </com.google.android.material.chip.ChipGroup>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/expenseList_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/expenseChipGroup_chipGroup"
        tools:listitem="@layout/item_expense" />

</androidx.constraintlayout.widget.ConstraintLayout>